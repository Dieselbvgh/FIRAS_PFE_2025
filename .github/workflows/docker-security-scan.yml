name: 🔍 Docker Security Scan & Rebuild

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  security-scan-rebuild:
    runs-on: self-hosted
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐳 Setup Docker
      run: |
        sudo systemctl start docker
        sleep 2

    - name: 🔄 Update Trivy Database
      run: |
        echo "🔄 Updating Trivy vulnerability database..."
        sudo trivy image --download-db-only
        echo "✅ Database update completed"

    - name: 🔍 Scan OLD Node.js (Has Vulnerabilities)
      run: |
        echo "🔍 Scanning node:14-alpine (older version with vulnerabilities)..."
        
        # Use older Node.js version that has vulnerabilities
        sudo docker pull node:14-alpine
        
        # Scan with full database
        sudo trivy image --format json node:14-alpine > scan-original.json
        
        # Count vulnerabilities from JSON
        OLD_CRITICAL=$(grep -o '"Severity":"CRITICAL"' scan-original.json | wc -l)
        OLD_HIGH=$(grep -o '"Severity":"HIGH"' scan-original.json | wc -l)
        OLD_MEDIUM=$(grep -o '"Severity":"MEDIUM"' scan-original.json | wc -l)
        
        echo "📊 ORIGINAL IMAGE:"
        echo "  Critical: $OLD_CRITICAL"
        echo "  High: $OLD_HIGH" 
        echo "  Medium: $OLD_MEDIUM"
        
        # Track in dashboard
        curl -X POST http://localhost:3000/api/deployments/track \
          -H "Content-Type: application/json" \
          -d "{\"status\": \"vulnerability-scan\", \"source\": \"security-scan\", \"commit\": \"${{ github.sha }}\", \"vulnerabilities_original\": $OLD_CRITICAL}" || echo "Tracking failed but continuing"

    - name: 🛠️ Build Secure Version
      run: |
        echo "🛠️ Building hardened version..."
        
        # Create hardened Dockerfile
        cat > Dockerfile.secure << 'DOCKERFILE'
        FROM node:14-alpine
        
        # Update ALL packages (this fixes most vulnerabilities)
        RUN apk update && apk upgrade --no-cache
        
        # Remove unnecessary packages to reduce attack surface
        RUN apk del curl wget 2>/dev/null || echo "Packages not present"
        
        # Use non-root user for security
        RUN addgroup -g 1001 -S appuser && \
            adduser -S appuser -u 1001 -G appuser
            
        # Switch to non-root user
        USER appuser
        
        # Clean up
        RUN rm -rf /var/cache/apk/*
        DOCKERFILE
        
        sudo docker build -f Dockerfile.secure -t node-secure-$(date +%s) .
        echo "✅ Hardened image built"

    - name: 🔍 Scan Secure Image
      run: |
        echo "🔍 Scanning the hardened image..."
        
        # Find the secure image
        SECURE_IMAGE=$(sudo docker images --format "table {{.Repository}}:{{.Tag}}" | grep node-secure | head -1)
        echo "Scanning: $SECURE_IMAGE"
        
        sudo trivy image --format json "$SECURE_IMAGE" > scan-secure.json
        
        NEW_CRITICAL=$(grep -o '"Severity":"CRITICAL"' scan-secure.json | wc -l)
        NEW_HIGH=$(grep -o '"Severity":"HIGH"' scan-secure.json | wc -l)
        NEW_MEDIUM=$(grep -o '"Severity":"MEDIUM"' scan-secure.json | wc -l)
        
        echo "📊 SECURE IMAGE:"
        echo "  Critical: $NEW_CRITICAL"
        echo "  High: $NEW_HIGH"
        echo "  Medium: $NEW_MEDIUM"

    - name: 📊 Show Results
      run: |
        echo "========================================"
        echo "🎉 SECURITY SCAN RESULTS"
        echo "========================================"
        echo "Image: node:14-alpine"
        echo ""
        echo "🔓 BEFORE HARDENING:"
        echo "  Critical: $OLD_CRITICAL"
        echo "  High: $OLD_HIGH"
        echo "  Medium: $OLD_MEDIUM"
        echo ""
        echo "🔒 AFTER HARDENING:"
        echo "  Critical: $NEW_CRITICAL"
        echo "  High: $NEW_HIGH" 
        echo "  Medium: $NEW_MEDIUM"
        echo ""
        
        CRITICAL_REDUCTION=$((OLD_CRITICAL - NEW_CRITICAL))
        HIGH_REDUCTION=$((OLD_HIGH - NEW_HIGH))
        MEDIUM_REDUCTION=$((OLD_MEDIUM - NEW_MEDIUM))
        
        echo "📉 VULNERABILITY REDUCTION:"
        echo "  Critical: -$CRITICAL_REDUCTION"
        echo "  High: -$HIGH_REDUCTION"
        echo "  Medium: -$MEDIUM_REDUCTION"
        
        TOTAL_REDUCTION=$((CRITICAL_REDUCTION + HIGH_REDUCTION + MEDIUM_REDUCTION))
        
        if [ $TOTAL_REDUCTION -gt 0 ]; then
          echo ""
          echo "✅ SUCCESS: $TOTAL_REDUCTION vulnerabilities fixed!"
          echo "🔒 Security hardening effective!"
        else
          echo ""
          echo "⚠️  No vulnerability reduction detected"
          echo "💡 The image might already be updated in registry"
        fi
        echo "========================================"
        
        # Track final results
        curl -X POST http://localhost:3000/api/deployments/track \
          -H "Content-Type: application/json" \
          -d "{\"status\": \"security-scan-complete\", \"source\": \"security-pipeline\", \"commit\": \"${{ github.sha }}\", \"vulnerabilities_before\": $OLD_CRITICAL, \"vulnerabilities_after\": $NEW_CRITICAL, \"reduction\": $CRITICAL_REDUCTION}" || echo "Final tracking failed"

    - name: 🧹 Cleanup
      run: |
        # Remove scan files
        rm -f scan-original.json scan-secure.json Dockerfile.secure || true
        
        # Remove Docker images to save space
        sudo docker rmi -f $(sudo docker images -q node-secure-*) 2>/dev/null || true
        sudo docker rmi node:14-alpine 2>/dev/null || true
        
        echo "🧹 Cleanup completed"
