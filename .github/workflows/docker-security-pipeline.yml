name: 🔒 Docker Security Scan & Rebuild Pipeline

on:
  workflow_dispatch:  # Allows manual trigger
  push:
    branches: [ main ]

jobs:
  docker-security-pipeline:
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐳 Set up Docker
      run: |
        echo "🔧 Setting up Docker..."
        sudo systemctl start docker || true
        sleep 2

    - name: 🔍 Quick Scan Original Image
      run: |
        echo "🔍 Step 1: Quick scanning nginx:latest..."
        
        # Pull the image
        sudo docker pull nginx:latest
        
        # Run QUICK Trivy scan (skip DB download, only vuln scanner)
        echo "🚀 Running quick scan (this may take 2-3 minutes)..."
        sudo trivy image --scanners vuln --skip-db-update --skip-java-db-update nginx:latest > scan-output.txt 2>&1 || echo "Scan completed"
        
        # Count vulnerabilities from output
        CRITICAL_COUNT=$(grep -o "CRITICAL" scan-output.txt | wc -l || echo "0")
        HIGH_COUNT=$(grep -o "HIGH" scan-output.txt | wc -l || echo "0")
        
        echo "📊 Quick Scan Results:"
        echo "  Critical vulnerabilities: $CRITICAL_COUNT"
        echo "  High vulnerabilities: $HIGH_COUNT"
        
        # Track scan in dashboard
        curl -X POST http://localhost:3000/api/deployments/track \
          -H "Content-Type: application/json" \
          -d '{
            "status": "scan-completed", 
            "source": "docker-security-pipeline",
            "commit": "'${{ github.sha }}'",
            "scan_results": {
              "critical": '$CRITICAL_COUNT',
              "high": '$HIGH_COUNT'
            }
          }' || echo "Scan tracking completed"

    - name: 🛠️ Build Hardened Docker Image
      run: |
        echo "🛠️ Step 2: Building hardened Docker image..."
        
        # Create a security-hardened Dockerfile
        cat > Dockerfile.hardened << 'EOF'
        FROM nginx:latest
        
        # Security updates - update all packages (main security improvement)
        RUN apt-get update && apt-get upgrade -y && \
            apt-get clean && \
            rm -rf /var/lib/apt/lists/*
        
        # Remove any unnecessary packages that might exist
        RUN set -e; \
            for pkg in curl wget telnet netcat; do \
                if dpkg -l | grep -q "^ii.*$pkg "; then \
                    echo "Removing $pkg"; \
                    apt-get remove -y "$pkg"; \
                else \
                    echo "Package $pkg not installed, skipping"; \
                fi; \
            done; \
            apt-get autoremove -y && \
            apt-get clean
        
        # Create non-root user for security (important security improvement)
        RUN useradd -r -s /bin/false nginxuser && \
            chown -R nginxuser:nginxuser /var/cache/nginx && \
            chown -R nginxuser:nginxuser /var/log/nginx
        
        # Switch to non-root user
        USER nginxuser
        
        # Health check
        HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
          CMD curl -f http://localhost/ || exit 1
        
        EXPOSE 80
        CMD ["nginx", "-g", "daemon off;"]
        EOF
        
        # Build hardened image with timestamp
        TIMESTAMP=$(date +%Y%m%d-%H%M%S)
        sudo docker build -f Dockerfile.hardened -t nginx-hardened-$TIMESTAMP .
        
        echo "✅ Hardened image built: nginx-hardened-$TIMESTAMP"

    - name: 🔍 Quick Scan Hardened Image
      run: |
        echo "🔍 Step 3: Quick scanning hardened image..."
        
        # Find the latest hardened image
        HARDENED_IMAGE=$(sudo docker images --format "table {{.Repository}}:{{.Tag}}" | grep nginx-hardened | head -1)
        
        if [ -z "$HARDENED_IMAGE" ]; then
          echo "❌ No hardened image found"
          exit 1
        fi
        
        echo "📦 Scanning hardened image: $HARDENED_IMAGE"
        
        # Quick scan of hardened image
        sudo trivy image --scanners vuln --skip-db-update --skip-java-db-update $HARDENED_IMAGE > hardened-scan-output.txt 2>&1 || echo "Hardened scan completed"
        
        # Count vulnerabilities in hardened image
        NEW_CRITICAL=$(grep -o "CRITICAL" hardened-scan-output.txt | wc -l || echo "0")
        NEW_HIGH=$(grep -o "HIGH" hardened-scan-output.txt | wc -l || echo "0")
        
        echo "📊 Hardened Scan Results:"
        echo "  Critical vulnerabilities: $NEW_CRITICAL"
        echo "  High vulnerabilities: $NEW_HIGH"

    - name: 📊 Compare Results & Final Report
      run: |
        echo "📊 Step 4: Generating security report..."
        
        # Get original scan results
        OLD_CRITICAL=$(grep -o "CRITICAL" scan-output.txt | wc -l || echo "0")
        OLD_HIGH=$(grep -o "HIGH" scan-output.txt | wc -l || echo "0")
        
        # Get hardened scan results  
        NEW_CRITICAL=$(grep -o "CRITICAL" hardened-scan-output.txt | wc -l || echo "0")
        NEW_HIGH=$(grep -o "HIGH" hardened-scan-output.txt | wc -l || echo "0")
        
        # Calculate reduction
        CRITICAL_REDUCTION=$((OLD_CRITICAL - NEW_CRITICAL))
        HIGH_REDUCTION=$((OLD_HIGH - NEW_HIGH))
        
        # Calculate improvement percentage
        if [ $OLD_CRITICAL -gt 0 ]; then
          CRITICAL_IMPROVEMENT=$((CRITICAL_REDUCTION * 100 / OLD_CRITICAL))
        else
          CRITICAL_IMPROVEMENT=0
        fi
        
        if [ $OLD_HIGH -gt 0 ]; then
          HIGH_IMPROVEMENT=$((HIGH_REDUCTION * 100 / OLD_HIGH))
        else
          HIGH_IMPROVEMENT=0
        fi
        
        echo "=========================================="
        echo "🎉 DOCKER SECURITY PIPELINE COMPLETE!"
        echo "=========================================="
        echo "📦 Original Image: nginx:latest"
        echo "🛡️ Hardened Image: nginx-hardened-*"
        echo ""
        echo "📊 VULNERABILITY COMPARISON:"
        echo "  Critical: $OLD_CRITICAL → $NEW_CRITICAL (Reduced by $CRITICAL_REDUCTION - $CRITICAL_IMPROVEMENT%)"
        echo "  High: $OLD_HIGH → $NEW_HIGH (Reduced by $HIGH_REDUCTION - $HIGH_IMPROVEMENT%)"
        echo ""
        echo "🔒 SECURITY IMPROVEMENTS APPLIED:"
        echo "  ✅ All packages updated to latest versions"
        echo "  ✅ Unnecessary packages removed"
        echo "  ✅ Running as non-root user"
        echo "  ✅ Reduced attack surface"
        echo "  ✅ Health checks added"
        echo ""
        echo "📁 Scan results saved in workflow artifacts"
        echo "=========================================="
        
        # Track final results in dashboard
        curl -X POST http://localhost:3000/api/deployments/track \
          -H "Content-Type: application/json" \
          -d '{
            "status": "pipeline-completed", 
            "source": "docker-security-pipeline",
            "commit": "'${{ github.sha }}'",
            "pipeline_results": {
              "original_critical": '$OLD_CRITICAL',
              "original_high": '$OLD_HIGH',
              "hardened_critical": '$NEW_CRITICAL',
              "hardened_high": '$NEW_HIGH',
              "critical_reduced": '$CRITICAL_REDUCTION',
              "high_reduced": '$HIGH_REDUCTION',
              "critical_improvement_percent": '$CRITICAL_IMPROVEMENT',
              "high_improvement_percent": '$HIGH_IMPROVEMENT',
              "security_improvements": [
                "package_updates",
                "non_root_user", 
                "reduced_attack_surface",
                "health_checks"
              ]
            }
          }' || echo "Final tracking completed"

    - name: 📁 Upload Scan Results
      uses: actions/upload-artifact@v4
      with:
        name: security-scan-results
        path: |
          scan-output.txt
          hardened-scan-output.txt
        retention-days: 7

    - name: 🧹 Cleanup
      run: |
        echo "🧹 Cleaning up temporary files..."
        rm -f scan-output.txt hardened-scan-output.txt Dockerfile.hardened || true
        echo "✅ Pipeline cleanup completed"
